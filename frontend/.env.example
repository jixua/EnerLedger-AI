# 本地开发默认通过 Vite 将 /api 与 /health 代理到 Python 服务。
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000
VITE_API_BASE_URL=
VITE_API_USER_ID=1

# 只有需要独立展示前端预览数据时才设为 true；真实联调必须保持 false。
VITE_DEMO_MODE=false
VITE_DOCUMENT_POLL_INTERVAL_MS=3000
