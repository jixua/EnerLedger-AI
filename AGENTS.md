# Repository Engineering Instructions

本文件适用于整个仓库。子目录中的 `AGENTS.md` 可以补充更具体的模块规范；若与本文件冲突，在对应子目录内优先遵循更具体的规范。用户和系统的明确指令始终具有更高优先级。

## Branch Roles

- `master` 是稳定主分支，只接收已经在 `dev` 完成验收的同一候选分支。
- `dev` 是集成和测试分支，可同时包含多个尚未发布的需求，不得将整个 `dev` 直接合并到 `master`。
- 需求、Bug 修复、重构、配置和文档变更都必须使用从最新 `origin/master` 迁出的独立候选分支。

## Mandatory Development Flow

1. 开始工作前同步 `origin/master`，确认基线、工作树状态和变更范围。
2. 从最新 `origin/master` 创建新分支。默认使用 `feature/<topic>`；Bug 修复可使用 `feature/fix-<topic>`。
3. 只在候选分支上修改和提交；禁止向 `master` 或 `dev` 直接提交、直接推送业务变更。
4. 完成开发和本地验证后，推送候选分支，首先创建“候选分支 → `dev`” PR。
5. PR 合并到 `dev` 后执行与风险相匹配的构建、自动化测试、集成测试和真实环境验收。记录候选分支的完整 SHA、Dev PR、测试结果和必要的健康检查证据。
6. Dev 验收通过后，使用同一候选分支创建“候选分支 → `master`” PR；禁止用“`dev` → `master`”代替发布 PR。
7. 从 Dev 验收开始到 Master PR 合并前，候选分支 HEAD SHA 必须保持不变。若追加任何提交，之前的 Dev 验收失效，必须重新执行“PR 到 `dev` → 测试”流程。
8. 候选分支在 Master PR 合并前不得删除。Master 合并完成后才可清理远程和本地候选分支。

## Pull Request Requirements

- PR 标题、提交信息和主体默认使用清晰的中文。
- PR 必须说明变更内容、根因或背景、影响范围、风险与验证证据。
- 只暂存当前任务文件；不得默认使用 `git add -A` 带入无关或用户未提交修改。
- 优先使用普通 merge commit 保留 PR 轨迹。不得为了绕过检查而强制推送、直接改写共享分支或跳过 Dev 验收。
- Master PR 必须引用 Dev PR 和验收结果，并核对其候选分支 SHA 与 Dev 验收时一致。

## Validation Baseline

- Python 变更至少运行相关 `pytest` 和 Ruff 检查；依赖、入口或跨模块变更应扩大至全量测试。
- 前端变更至少运行相关测试和 `npm run build`；发布前按项目现有契约运行完整前端测试。
- 数据库、消息队列、检索索引和部署配置变更必须执行真实依赖验收；未执行的步骤必须明确标记为未验证，不得推断成功。
- 纯文档变更可以进行路径、Markdown 和规范一致性检查，无需为了形式执行无关的重型测试。

## Safety and Repository Hygiene

- 保留用户现有的未提交变更、未跟踪文件和本地运行环境；未经授权不得清理或覆盖。
- 禁止提交密钥、Token、密码、本地 `.env` 或临时依赖目录。日志和命令输出不得泄露凭据值。
- 不得使用 `git reset --hard`、强制覆盖或其他破坏性命令处理未知状态。
- 部署、数据清理、数据库迁移、标签和发布均需要与用户授权的范围一致；PR 合并不等于已部署。
